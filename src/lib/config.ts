/**
 * Global configuration constants
 *
 * This file centralizes shared URLs and other static configuration values
 * so they can be easily reused and updated in one place.
 */



/** GITHUB_URL:
 *   - The canonical URL for the Reputator GitHub repository.
 *   - Used in multiple places across the app (e.g., Footer, Sidebar navigation)
 *   - Update this value here to change the GitHub link everywhere it is referenced.
 */
export const GITHUB_URL = 'https://github.com/fairtale5/Reputator'; 