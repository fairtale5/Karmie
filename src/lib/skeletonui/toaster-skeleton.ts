import { createToaster } from '@skeletonlabs/skeleton-svelte';

/**
 * Shared Skeleton toaster instance for app-wide toast notifications.
 *
 * @see https://www.skeleton.dev/docs/components/toast/svelte
 */
export const toaster = createToaster(); 